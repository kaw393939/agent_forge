# IMPORTANT: This is an example file. Copy to secrets.yaml and replace these placeholders with your actual keys.
# NEVER commit real credentials to version control.
secrets:
  dockerHubToken: "your-docker-hub-token-here"
  openaiApiKey: "your-openai-api-key-here"
  digitalOceanToken: "your-digitalocean-token-here"
